import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'LifeAI',
  description: '당신의 라이프스타일을 AI로 다시 설계하다',
  viewport: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no',
};
